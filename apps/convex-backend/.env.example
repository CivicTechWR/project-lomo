# Copy to .env.local and fill in. Convex CLI loads .env.local when you run `convex dev`.

CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=

# Required by convex/lib/siteEnv.ts — URL of the Next.js app (Better Auth). Local dev example:
SITE_URL=http://localhost:3000
